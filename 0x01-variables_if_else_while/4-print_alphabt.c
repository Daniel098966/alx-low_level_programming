#include <stdio.h>
/**
 * main - ommit alphabet
 *
 * Return: Always (Success)
 */
int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	{
		if (h != 'e' && c != 'q')
		{
			putchar(h);
		}
	}
	putchar('\n');
	return (0);
}
